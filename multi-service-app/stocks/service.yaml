apiVersion: v1
kind: Service
metadata:
  name: stocks-service
  namespace: stock-portfolio-namespace
spec:
  selector:
    app: stocks-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 5000
